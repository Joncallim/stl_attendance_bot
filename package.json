{
  "name": "attendance-bot",
  "version": "1.0.0",
  "description": "Telegram bot that collects attendance and writes responses to Google Sheets.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/bot.js && node --check src/config.js && node --check src/googleSheets.js && node --check src/storage.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "googleapis": "^144.0.0",
    "node-cron": "^4.1.0",
    "telegraf": "^4.16.3"
  }
}
